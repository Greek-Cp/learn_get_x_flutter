import 'package:flutter/material.dart';

class PageDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Column(children: [Text("anjay gw sad coeg")]),
    );
  }
}
